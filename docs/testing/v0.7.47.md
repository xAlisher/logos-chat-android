# Testing plan — v0.7.47 (build 59)

Installed to all three phones (RedMe, Samsung, Pixel). Multi-agent batch: tsc clean,
175/175 logic tests pass, `assembleRelease` OK, `install -r` Success on each.

Tick each box on-device. Anything that fails → reopen the linked issue with a note.

## A. Single-phone UI (any one phone)

- [ ] **#256 Voice bubble** — record a ~20s note **and** the longest note you can on the
  same screen. Neither overflows the bubble; the short one stays compact; the long one
  visibly hits the width cap with the ▶ button + `mm:ss` still fully visible. Rotate to
  landscape and re-check (width now tracks screen size).
- [ ] **#253 Context FAB** — switch sections in the side menu (Logos / MeshCore /
  Bluetooth). The "+" recolors: **orange** (Logos) / **green** (MeshCore) / **blue**
  (BLE). Logos → *Contact* opens Scan, *Group* opens New Group. MeshCore items
  (*Public / Join private / Create private channel, New MeshCore contact*) land on the
  MeshCore page. BLE → *Add BLE Groups* is dimmed + non-tappable ("coming soon").
- [ ] **#182 MeshInfoModal** — open "About mesh mirroring" (mesh banner ⓘ, or chat ⋮).
  A persistent scrollbar shows and content scrolls fully to the bottom above "Got it".
- [ ] **#259 BLE restore after restart** — engage BLE mesh → force-kill the app →
  relaunch → BLE **auto re-engages** with the same *Broadcast identity* toggle state.
  Then: leave BLE **off**, restart → it **stays off**. Then: turn Bluetooth off at the
  OS level, restart → boot does **not** try to engage (no crash/error).

## B. Two-phone (need a peer phone)

- [ ] **#240 QR include-label** — on phone A set a label (display name), open My-address:
  the **"Include my label in QR"** toggle appears *only when a label is set*, default
  **off**. Turn it on → phone B scans → B's **new-conversation name is prefilled** with
  A's label. Backward-compat: B scans an **old bare-address QR** → still starts a
  conversation normally.
- [ ] **#210 Map-to-mesh from a message** — long-press a **peer's** bubble in a **1:1**
  and in a **group**. The item reads *"Map to mesh"* (unmapped) or *"Change mesh
  identity"* (already mapped) and opens MeshMapModal pre-targeting that sender. Mapping
  persists after reopening the chat. (New this build: the 1:1 case reads the mesh-map
  cache correctly.)
- [ ] **#252 Fresh-group join ordering** (carryover to confirm) — create a **new** group,
  invite a peer. On the invitee, the creator's **"joined"** system line sorts **before**
  the creator's first message, and **both** members show "joined" **without the invitee
  sending anything** (within ~10s).

## Already verified in code (closed, spot-check only if you like)
- #229 InfoModal scroll · #233 transport pill hides offline secondaries · #227 offline
  MeshCore/BLE icons render gray (Logos stays red).

## Not in this build — needs hardware / a VPS / a work session (still open)
- **#219** self-hosted delivery node (VPS) · **#254** MeshCore node-config parity ·
  **#186** radio picker · **#168** MeshCore group-mirror — need the LoRa radio / a server.
- **#248** BLE photos/voice · **#209** photos not delivered (Waku size) · **#195**
  invitee never joins (no subscribed peers) — need the delivery/BLE path + two phones.
- **#258** encrypt data at rest (Keystore + SQLCipher) · **#175/#176** reinstalled-peer
  reconcile · **#226** crash on resume — larger, separate work sessions.
