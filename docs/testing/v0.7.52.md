# Testing plan — v0.7.52 (build 64) · batch #2

All three phones on 0.7.52. tsc clean, 175/175 logic tests, `assembleRelease` OK,
`install -r` Success on Pixel + Samsung + RedMe, node opens cleanly. No libchat/.so
change this batch (only app-side JS + one Kotlin RN method). Tick each on-device.

## A. Single-phone

- [ ] **#257 recording waveform** — hold the mic to record a voice note. The record
  bar shows **28 red bars animating** smoothly (fills the space between the timer and
  Cancel/Send). Cancel and Send still work. Watch RedMe/Samsung (lower RAM) for jank.
  *(Synthetic animation — the native recorder doesn't expose live mic level to JS.)*
- [ ] **#241 share QR as image** — My-address → **Share** → the OS share sheet opens;
  share to Photos/Files/a note and confirm you get the **QR + sigil PNG image** (not
  just text). With the "Include my label" toggle on, the QR still encodes the label.
- [ ] **#236 auto-lock on background** — Settings → Security → **"Lock when app goes to
  background"** (only shown when a PIN is set), turn it **on**. Background the app for
  **>15 s**, return → the PIN gate appears. Quick app-switch (**<15 s**) → **no** relock.
  Toggle off → no relock.
- [ ] **#158 swipe-back** — on Chat / Group Info / Contacts / About, swipe from the left
  edge → navigates back. Confirm it doesn't fight swipe-to-delete on the conversation list.
- [ ] **#226 resume stability** — (dev) turn on "Don't keep activities", background then
  resume the app repeatedly → no black screen / no "Screen fragments" crash.

## B. Lists (visual)

- [ ] **#174 mesh badge** — a mesh-mapped 1:1 conversation row shows the green mesh badge
  (truncates cleanly next to long names). Contacts list already shows it.
- [ ] **#246 BLE presence** — a Bluetooth DM row shows a blue pulsing **"nearby"** dot when
  the peer is heard over BLE, and it clears when they go out of range. Check a row that's
  both mesh-mapped AND BLE doesn't crowd the title.

## Verified already-present (no change this batch)
- #201 Forward / Copy / Save · #190 named-bridge attribution.

## Known follow-ups (NOT in this build)
- **In-chat** placement of #174 mesh badge + #246 BLE presence in the ChatScreen header —
  deferred (that file was owned by another agent this batch).
- **#236 FLAG_SECURE / recents cover** (native, unshipped) — a locked app can still show
  the conversation list in the app-switcher thumbnail.
- **#257 real amplitude** — a mic-level-driven waveform needs a native amplitude event
  (current is synthetic).
- **#241** — verify it truly shares an image on all three launchers; if a launcher can't
  capture the Svg, it falls back to sharing the address text/URI.
