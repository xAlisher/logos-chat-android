# Testing plan — v0.7.53 (build 65) · batch #3

All three phones on 0.7.53. tsc clean, 175/175 tests, build OK, `install -r` Success ×3,
node opens cleanly. No libchat/.so change (app-side JS + one native RN module method).

## New this batch

- [ ] **#257 real-amplitude waveform** — record a voice note and **speak loudly, softly,
  then go silent**: the 28 bars should track real mic loudness (tall on loud, low on
  quiet/silence) rather than a fixed animation. Confirm no jank on RedMe/Samsung. If run
  against an un-rebuilt native lib it silently falls back to the synthetic animation — on
  0.7.53 it should be live amplitude.
- [ ] **#147 member picker presence** — New group → Add members (with BLE engaged + a
  peer nearby): header shows **"via mesh · N of M nearby"**; out-of-range members are
  dimmed with a **"not nearby"** caption, in-range show blue **"nearby"**. Note: only
  resolves peers you already have as 1:1 contacts. (Fully verifies with the radio/BLE
  peers present.)

## Verified already-present (spot-check only)
- **#230** invitation/join system lines (join clears "hasn't joined"; no stacked blocks
  on re-invite/restart) — unit-proven (34/34 + 10/10); confirm the end-to-end lifecycle
  on a real group join, esp. the late-join-via-welcome-catch-up reaching the creator.
- **#174** in-chat mesh badge (green, header) · **#246** in-chat BLE "● nearby" (blue, header).

## Still-open follow-ups (not attempted — deliberate)
- **#236 FLAG_SECURE / recents cover** — skipped: it blocks `adb screencap`, which would
  break the on-device verification loop. Revisit when screenshots aren't needed for testing.
